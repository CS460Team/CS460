import { WeekEventsPipe } from './week-events.pipe';

describe('WeekEventsPipe', () => {
  it('create an instance', () => {
    const pipe = new WeekEventsPipe();
    expect(pipe).toBeTruthy();
  });
});
